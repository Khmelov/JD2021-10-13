package by.it.oliakhevich.jd01_02;

public class TaskC {

    static void step3(int[][] array) {
        int max = Integer.MIN_VALUE;
        for (int[] row : array) {
            for (int[] element : array) {
                //   if (element > max) {
                //     max = element;
                //        }

                //      }

                //}
                // boolean[] skipCols = new boolean[array[0].length];
                //boolean[] skipRows = new boolean[array.length];
                // фото в   телефоне есть продолжения
                // for (int i = 0; i < array.length; i++) {
                //        for (int j = 0; j < array.length; j++) {
                //       if (array[i][j] == max) {
                //         skipCols[j] = true;
                //       skipRows[i] = true;

                //              }


                //}
                //}
                //int rows = 0;
                // for (boolean skipRow : ) {

                //}
            }
        }
    }
}